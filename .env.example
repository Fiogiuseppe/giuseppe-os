# Server-only AI control — mock (default) | live (requires ANTHROPIC_API_KEY on server)
AI_MODE=mock

# Brain API provider (claude | openai | gemini | local | rule-based)
BRAIN_AI_PROVIDER=claude

# Claude (only when AI_MODE=live is set on the server)
ANTHROPIC_API_KEY=