# Phase 2+ Sources code (inactive)

This folder contains work prepared for later phases. It is **excluded from TypeScript and builds** until activated phase by phase.

| Folder | Phase |
|--------|-------|
| `platform/` | 2 — Sources Engine |
| `connectors/` | 3–5 — Real connectors |
| `adapters/` | 9 — OAuth strategies |
| `oauth/` | 9 — OAuth routes helpers |
| `connection-store.server.ts` | 2 — Persistence bridge |

Do not import from `_phase2` in Phase 1 code. See `docs/SOURCES_ROADMAP.md`.
