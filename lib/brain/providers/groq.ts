/**
 * @deprecated Use getAIProvider() from lib/ai/orchestrator.
 * Kept for backward-compatible imports only.
 */
export { createGroqCompletionProvider as createGroqProvider } from '../../ai/completionAdapter';
